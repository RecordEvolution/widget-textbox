export interface InputData {
    title: Block
    subTitle: Block
    body: Block
}
export interface Block {
    text: string,
    fontSize: string,
    fontWeight: string
    color: string
    backgroundColor: string
}
